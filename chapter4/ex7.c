#include <stdio.h>

int main(int argc, char *argv[])
{
    int i;
    /* while (i = 2){ */
    /* 	printf("Some even numbers: %d %d %d\n", i, i + 2, i + 4); */
    /* 	i = 0; */
    /* } */
    if (i = 2)
	printf("Some even numbers: %d %d %d\n", i, i + 2, i + 4);
    return 0;
}
